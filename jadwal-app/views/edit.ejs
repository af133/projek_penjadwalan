<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Edit Jadwal</title>
</head>
<body>
    <h1>Edit Jadwal: <%= schedule.title %></h1>
    <form action="/jadwal/<%= schedule.id %>/edit" method="POST">
        <label>Judul:
            <input type="text" name="title" value="<%= schedule.title %>" required>
        </label>
        <br>
        <label>Tanggal:
            <input type="date" name="date" value="<%= schedule.date %>" required>
        </label>
        <br>
        <label>Deskripsi:
            <textarea name="description"><%= schedule.description %></textarea>
        </label>
        <br>
        <button type="submit">Simpan Perubahan</button>
    </form>
</body>
</html>
