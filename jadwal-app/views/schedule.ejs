<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Jadwal: <%= schedule.title %></title>
</head>
<body>
    <h1>Jadwal: <%= schedule.title %></h1>
    <p>Tanggal: <%= schedule.date %></p>
    <p>Deskripsi: <%= schedule.description %></p>

    <!-- Tombol Share via WhatsApp -->
    <p>
        <a href="https://wa.me/?text=Cek%20jadwal%20ini:%20<%= encodeURIComponent('http://localhost:3000/jadwal/' + schedule.id) %>" target="_blank">
            Bagikan via WhatsApp
        </a>
    </p>

    <!-- Tombol Edit -->
    <p>
        <a href="/jadwal/<%= schedule.id %>/edit">Ubah Jadwal</a>
    </p>

    <!-- Tombol Hapus -->
    <form action="/jadwal/<%= schedule.id %>/delete" method="POST" onsubmit="return confirm('Yakin ingin menghapus jadwal ini?');">
        <button type="submit">Hapus Jadwal</button>
    </form>
</body>
</html>
